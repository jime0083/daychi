# Daychi COFFEE MAP - 問題管理 (problem.txt)
# 運用ルール:
# - 開発中に見つかった問題(想定した動きをしない、デザイン崩れ等)は全てここに記録する
# - 修正プロセス(CLAUDE.md「問題修正プロセス」参照・省略禁止):
#   問題共有 → problem.txtに記録 → 原因を徹底調査 → 原因特定 →
#   タスクを切り出しprogress.txt/manual-work.txtに追加 → 修正作業
# - 問題IDは P-001 から連番で付与する
# - 原因調査・特定・切り出しタスクの欄を埋めずに修正作業を開始してはならない

=====================================
記録フォーマット(コピーして使用)
=====================================
## P-XXX: (問題タイトル)
- 記録日: YYYY-MM-DD
- 状態: 未着手 | 調査中 | 修正中 | 解決済み
- 発生箇所: (画面/機能/タスクID)
- 問題内容:
  (想定した動き / 実際の動き を具体的に)
- 再現手順:
  1. ...
- 原因調査:
  (調査した内容・確認したコード・データフローの追跡結果)
- 特定した原因:
  (根本原因。調査完了までは空欄)
- 切り出したタスク:
  (progress.txt / manual-work.txt に追加したタスクID)
- 解決日: YYYY-MM-DD
- 解決内容:
  (実施した修正と検証結果)

=====================================
問題一覧
=====================================

## P-001: Firestore Emulatorに必要なJava実行環境がPATHに存在しない
- 記録日: 2026-09-19
- 状態: 解決済み
- 発生箇所: タスク1-3(Firebase Emulator Suite設定)
- 問題内容:
  想定した動き: java -version が通り、Firestore Emulator(JRE依存)が起動できる
  実際の動き: 「Unable to locate a Java Runtime.」でjavaコマンドが失敗し、Emulatorが起動できない
- 再現手順:
  1. ターミナルで java -version を実行 → エラー
- 原因調査:
  - /usr/libexec/java_home も失敗し、システム標準のJavaランタイムは未インストール
  - Homebrewでは /opt/homebrew/opt/openjdk (openjdk 26.0.1) がインストール済みで、
    直接パス指定での動作確認は成功(/opt/homebrew/opt/openjdk/libexec/openjdk.jdk/Contents/Home/bin/java)
  - ただし brew link されておらず /opt/homebrew/bin に露出していないためPATHから解決できない
- 特定した原因:
  Homebrew版openjdkがインストール済みだが未リンク(keg-only)のため、javaコマンドがPATH上に存在しない
- 切り出したタスク: manual-work.txt Work 9(Java実行環境のPATH設定)
- 解決日: 2026-09-19
- 解決内容:
  ユーザー承認のうえ brew link --force --overwrite openjdk を実行(37 symlinks created)。
  java -version で openjdk 26.0.1 が返ることを確認。状態を「解決済み」とする

## P-002: クリーンな状態で npx tsc --noEmit が LayoutProps 未定義エラーで失敗する
- 記録日: 2026-09-19
- 状態: 解決済み
- 発生箇所: src/app/layout.tsx:20(タスク1-3実装中に発覚。1-3の変更とは無関係)
- 問題内容:
  想定した動き: 完了前必須チェックの npx tsc --noEmit がいつでもパスする
  実際の動き: .next ディレクトリが無い状態では
  「src/app/layout.tsx(20,50): error TS2304: Cannot find name 'LayoutProps'.」で失敗する
- 再現手順:
  1. .next ディレクトリを削除(またはクリーンな状態でclone)
  2. npx tsc --noEmit を実行 → TS2304エラー
- 原因調査:
  - layout.tsx:20 は create-next-app(Next.js 16)標準テンプレートのまま LayoutProps<"/"> を使用
  - LayoutProps は Next.js 16 が next dev / next build / next typegen 実行時に
    .next/types へ自動生成するグローバル型(tsconfigのincludeで取り込まれる)
  - 現在 .next が存在しないことを確認し、エラーを再現
  - タスク1-1/1-2の検証時は devサーバー起動後(.next生成済み)だったため偶然パスしていた
- 特定した原因:
  自動生成型 LayoutProps に依存するコードに対し、型生成(next typegen)を伴わない
  素の tsc --noEmit を実行していたため。チェックコマンド側の手順不足が根本原因
- 切り出したタスク: progress.txt タスク1-7(型チェックコマンドの安定化)
- 解決日: 2026-09-19
- 解決内容:
  npm script "typecheck"(next typegen && tsc --noEmit)を追加(コミット c863078)。
  .next 削除済みのクリーン状態から npm run typecheck が2回連続でパスすることを
  実装・レビュー双方で確認。以後の型チェックは npm run typecheck に統一
  (CLAUDE.mdのコマンド表も更新済み)

## P-003: セキュリティルール適用後にseedスクリプトがPERMISSION_DENIEDで失敗
- 記録日: 2026-09-19
- 状態: 解決済み
- 発生箇所: scripts/seed.ts(タスク1-6のnpm run e2e初回実行時に顕在化)
- 問題内容:
  想定した動き: npm run e2e のglobalSetupでseedデータがEmulatorに投入される
  実際の動き: seed投入が PERMISSION_DENIED で失敗する
- 再現手順:
  1. タスク1-5適用後(firestore.rules本実装後)の状態で emulator起動
  2. npm run seed を実行 → PERMISSION_DENIED
- 原因調査:
  - タスク1-5の firestore.rules は write を管理者(admin==trueクレーム)のみに制限
  - タスク1-3作成の scripts/seed.ts は未認証のclient SDKで書き込む実装だった
  - 1-5のレビューではリポジトリテスト(管理者サインイン済み)のみ通しで確認され、
    seedスクリプト単体はルール適用後に未検証だったため、1-6の全自動フローで初めて顕在化
- 特定した原因:
  firestore.rules(1-5)と seed.ts(1-3)の設計不整合。seed.tsが無認証書き込みのままだった
- 切り出したタスク: タスク1-6の範囲内で修正(E2E基盤の完了条件に必須のため)
- 解決日: 2026-09-19
- 解決内容:
  scripts/seed.ts を @firebase/rules-unit-testing の withSecurityRulesDisabled()方式
  (ルールテストで確立済みのパターン)に変更。あわせてFirestore接続残留でnodeが
  終了しない問題に process.exit() を明示。npm run e2e 3回連続グリーンで解決確認。
  検証はタスク1-6のレビュー(daychi-review)に含めて実施

## P-004: npm run lint が e2e/artifacts の生成物を走査して大量エラーになる
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: eslint.config.mjs(タスク2-1の検証中に発覚)
- 問題内容:
  想定した動き: npm run lint がプロジェクトのソースのみを対象にパスする
  実際の動き: npm run e2e 実行後、e2e/artifacts/html-report 配下のPlaywright生成JS
  (圧縮済み)をESLintが走査し、259エラー・約2795警告で失敗する
- 再現手順:
  1. npm run e2e を1回実行(e2e/artifacts/html-report が生成される)
  2. npm run lint を実行 → 生成物由来の大量エラーで失敗
- 原因調査:
  - eslint.config.mjs の globalIgnores は .next/**, out/**, build/**, next-env.d.ts のみ
    を除外しており、e2e/artifacts/** が除外対象に入っていない
  - ESLint(flat config)は .gitignore を自動参照しないため、gitignore済みでも走査される
  - npx eslint src e2e --ignore-pattern 'e2e/artifacts/**' は exit 0(2-1コード自体は健全)
    → 失敗はすべて生成物由来と確認
  - 1-6のレビューでlintがパスしたのは、lint実行時点でartifactsが未生成だった等の
    実行順序に依存していたため。恒常的な設定不足が根本原因
- 特定した原因:
  eslint.config.mjs の除外リストに e2e/artifacts(および playwright-report / test-results)
  が含まれていないこと。lintの対象範囲設定の不足
- 切り出したタスク: progress.txt タスク1-8(lint除外設定の修正)
- 解決日: 2026-09-22
- 解決内容:
  eslint.config.mjs の globalIgnores に e2e/artifacts/**、playwright-report/**、
  test-results/** を追加(コミット b75cc29)。artifacts生成済みの状態で npm run lint が
  exit 0 でパスすること、実コード(src/e2e配下)は引き続きlint対象であることを
  実装・レビュー双方で確認

## P-005: /admin がクライアント側で auth/invalid-api-key でクラッシュする
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: src/lib/firebase-config.ts(Phase 1のタスク1-2/1-4由来の潜在バグ。タスク2-1のE2E 6件失敗として顕在化)
- 問題内容:
  想定した動き: /admin がブラウザで表示され、ログイン/アクセス拒否/管理画面の3分岐が動く
  実際の動き: クライアント側レンダリング時に getAuth() が auth/invalid-api-key を投げ、
  UIが一切描画される前にページ全体がクラッシュ。admin-auth.spec.ts が6件(3シナリオ×2プロジェクト)失敗
- 再現手順:
  1. NEXT_PUBLIC_USE_EMULATOR=true で npm run e2e を実行
  2. admin-auth.spec.ts の全シナリオが同一原因で失敗
- 原因調査:
  - firebase-config.ts の関数群が env: NodeJS.ProcessEnv = process.env を引数に取り、
    本体で env.NEXT_PUBLIC_X と間接参照していた
  - Next.js/Turbopack はクライアントバンドルへ「リテラルな process.env.NEXT_PUBLIC_X」
    のみをインライン展開する。エイリアス経由(env.NEXT_PUBLIC_X)は静的解析で見えず
    undefined になる
  - 結果、ブラウザ側で全 NEXT_PUBLIC_* が undefined となり getAuth() が例外
  - Phase 1ではクライアントからこの初期化を通る画面が無かったため潜在化していた
- 特定した原因:
  クライアント向けFirebase設定の環境変数を間接参照していたため、Next.jsの静的置換が
  効かずクライアントバンドルで値が欠落したこと
- 切り出したタスク: タスク2-1の範囲内で修正(2-1の完了条件E2Eに必須のため)
- 解決日: 2026-09-22
- 解決内容:
  firebase-config.ts を、リテラルな process.env.NEXT_PUBLIC_X を直接書く readEnv()
  ヘルパー方式に変更(呼び出しごとに評価。テストのprocess.env変更も反映)。
  Auth Emulatorモード用に demo API key フォールバックも追加。npm run e2e 8件全通過で
  解決確認。検証はタスク2-1のレビュー(daychi-review)に含めて実施。
  【今後の注意】クライアント側で NEXT_PUBLIC_* を読む場合は必ずリテラル参照にすること
  (2-2以降で新たな env 依存のクライアント設定を追加する場合に留意)

## P-006: maplibre-gl@5.24.0 に本番依存の critical 脆弱性(XSS)
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: package.json の dependencies(タスク2-4で追加)。タスク2-4のレビューでFAIL事由として検出
- 問題内容:
  想定した動き: 本番依存に critical/high 脆弱性を持ち込まない
  実際の動き: タスク2-4で追加した maplibre-gl@5.24.0 に critical 脆弱性
  GHSA-jrc7-96c5-q579(XSS Sanitizer Bypass in DOM.sanitize())が含まれる
- 再現手順:
  1. npm audit を実行 → critical 1件(maplibre-gl@5.24.0、isDirect:true、dependencies)
- 原因調査:
  - タスク2-4(コミット 8351293)で地図UI用に maplibre-gl@^5.24.0 を追加した
  - 5.24.0 は GHSA-jrc7-96c5-q579 の影響を受けるバージョン
  - 現状の実使用での実害経路は低い(Popup.setHTMLで未信頼HTMLを描画しておらず、
    draggable Marker と信頼できるOpenFreeMapスタイルURLのみ使用)が、脆弱コードは
    本番に同梱され、Phase 3(3-1 公開ページ地図)でも再利用されるため放置不可
  - moderate 8件は firebase-tools(dev依存、Phase 1導入)配下のtransitiveで本番非同梱・
    本問題とは別事象(別途 dev依存更新方針を検討。P-006の対象外)
- 特定した原因:
  脆弱性を含む maplibre-gl@5.24.0 を本番依存に追加したこと
- 切り出したタスク: progress.txt タスク2-4a(maplibre-gl のセキュリティ更新)
- 解決日: 2026-09-22
- 解決内容:
  maplibre-gl を 5.24.0 → 6.10.0 へ更新(コミット b87a467)。v6はESM/named export専用の
  ため ShopLocationPicker.tsx の import を「import * as maplibregl」に追随修正(それ以外の
  地図API使用箇所は無変更)。npm audit で critical 0件を確認。npm run e2e(desktop+mobile
  14件)2回連続グリーン、実ブラウザで地図描画・座標反映・SSRエラー無しをレビューで確認。
  moderate 8件(firebase-tools系dev依存)はP-006対象外の既存事象として別途方針検討

## P-007: performers-crud.spec.ts の表示順アサーションがフレーキー
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: e2e/performers-crud.spec.ts(タスク2-2で作成)。タスク2-6のレビュー中に発覚
- 問題内容:
  想定した動き: E2Eが決定的に(毎回)グリーンになる
  実際の動き: performers-crud(mobile)が約0.4%の確率でstrict modeのタイムアウトで失敗
- 再現手順:
  1. npm run e2e を多数回実行すると、まれに performers-crud.spec.ts が失敗する
- 原因調査:
  - performers-crud.spec.ts:49 で表示順に固定値 "42" を入力し、row.getByText("42") で
    部分一致検索している
  - uniqueTestId()(src/repositories/test-support.ts:65)が生成するランダムID
    (例 1789827262544-42c0xi)がたまたま "42" を部分文字列として含むと、
    Playwrightのstrict modeが2要素にマッチしてタイムアウトする
  - タスク2-6のdiffとは無関係の既存事象(コミット40766e8/タスク2-2由来)
- 特定した原因:
  テストの表示順アサーションが厳密ロケータでなく部分一致テキスト検索であり、
  ランダムIDと衝突しうること(テストコードの堅牢性不足)
- 切り出したタスク: progress.txt タスク2-2a(performers-crud E2Eのフレーキー修正)
- 解決日: 2026-09-22
- 解決内容:
  performers/page.tsx の表示順セルに data-testid="performer-order" を付与し、
  performers-crud.spec.ts のアサーションを row.getByTestId("performer-order") +
  toHaveText の厳密一致に変更(部分一致 getByText を廃止)。入力値も "42"→"917" に変更。
  コミット 72cea1b。npm run e2e を3回連続実行し22件グリーン(フレーキー再発なし)を
  実装・レビュー双方で確認

## P-008: next dev の Issues バッジが下部タブバーと重なりE2Eクリックを妨げる
- 記録日: 2026-09-20
- 状態: 解決済み(回避策適用、本番影響なし)
- 発生箇所: e2e/mobile-ui.spec.ts のタブクリック(タスク3-5実装中に発覚)
- 問題内容:
  想定した動き: モバイル下部タブ「地図」「動画一覧」のクリックがE2Eで安定して通る
  実際の動き: Next.js の next dev 専用「Issues」インジケータバッジ(左下)が
  下部タブバーと重なり、Playwrightの座標ベースクリックを間欠的にintercept してタイムアウト
- 再現手順:
  1. npm run e2e でモバイルのタブクリックを座標ベースで行うと間欠的に失敗
- 原因調査:
  - 既存の全mapスペックが地図タイル(tiles.openfreemap.org)を意図的にブロックしており、
    その結果 next dev の「Issues」バッジが表示される
  - このバッジは画面左下に固定表示され、下部タブバーと表示領域が重なる
  - next.config.ts の devIndicators: false では Next.js 16.3.5 でこのバッジを抑制できないことを
    実験で確認。.click({ force: true }) も座標ヒットテストを回避するだけで実クリックは最上位要素に当たり不可
  - バッジは next dev 専用で本番ビルドには存在しない(=本番のタブバー操作性には影響しない)
- 特定した原因:
  next dev 専用の開発用オーバーレイ(Issuesバッジ)がテスト環境でのみタブバーと重なること。
  本番コードの欠陥ではなくテスト環境固有の事象
- 切り出したタスク: タスク3-5の範囲内で対応(テスト専用ヘルパーで回避)
- 解決日: 2026-09-20
- 解決内容:
  e2e/mobile-ui.spec.ts にテスト専用ヘルパー clickBypassingDevOverlay() を追加し、
  対象DOMノードのネイティブ .click() を直接呼ぶ方式でタブバー2ボタンのみ回避。
  本番ビルドにバッジは存在しないため本番影響なし。回避が本番の実レイアウト欠陥を
  隠していないことはタスク3-5のレビュー(daychi-review)で本番ビルド観点も含めて検証

## P-009: detail-sheet.spec.ts が動的作成店舗とseed店舗のピン座標重なりで間欠失敗
- 記録日: 2026-09-20
- 状態: 解決済み
- 発生箇所: e2e/detail-sheet.spec.ts:150 付近(タスク3-2由来。タスク3-5のレビュー中に発覚)
- 問題内容:
  想定した動き: detail-sheet の複数visitテストが決定的に(毎回)通る
  実際の動き: 5回中1回程度、chromium-desktop でピンクリックがタイムアウトして失敗
- 再現手順:
  1. npm run e2e を5回程度繰り返すと稀に detail-sheet.spec.ts が失敗する
  2. または npx playwright test e2e/detail-sheet.spec.ts --repeat-each=20 --project=chromium-desktop
- 原因調査:
  - 複数visitテストは管理画面CRUDで新規店舗を動的作成する。座標にジッターを入れているが
    ランダムに seed店舗(shop-test-published-01)のピンと画面座標が近接/重なることがある
  - ピンが重なるとPlaywrightのクリックが目的のピンに届かずintercept/タイムアウトする
  - 3-4のフィルタ帯レイアウト修正後も、ピン同士の重なり自体は残存(フィルタとの重なりではなく
    ピン同士の重なりが原因)
- 特定した原因:
  テストデータの店舗座標が非決定的(ジッター)で、seed店舗ピンと画面上で重なりうること。
  テストの座標設計の堅牢性不足(本番コードの欠陥ではない)
- 切り出したタスク: progress.txt タスク3-2b(detail-sheet 複数visitテストの座標決定化)
- 解決日: 2026-09-20
- 解決内容:
  detail-sheet.spec.ts の動的作成店舗座標をランダムジッターから決定的な遠隔固定値
  (35.300000, 139.300000。seed店舗から40km超)に変更。加えて複数visitテスト専用の
  openDetailSheetByShopName() のクリックを evaluate(element.click())方式に堅牢化
  (通常ピンクリック検証テストは pin.click() のまま維持)。コミット c7c34f1。
  --repeat-each=20 で80 passed、npm run e2e(desktop+mobile)2回グリーンを実装・レビュー
  双方で確認。mobile skip解消は本タスク範囲外として見送り(3-2フォローアップに残置)

## P-010: .env.local の本番projectIdがEmulator E2Eに漏れ、seedデータとズレて21件失敗
- 記録日: 2026-09-21
- 状態: 解決済み
- 発生箇所: .env.local(メインエージェントがWork 1完了時に作成)/ playwright.config.ts
- 問題内容:
  想定した動き: npm run e2e が Emulator + seedデータで決定的に緑
  実際の動き: public-map等でpublished店舗ピンが toHaveCount(1) 期待に対し 0 になり
  desktop/mobile対称で21件失敗
- 再現手順:
  1. .env.local に NEXT_PUBLIC_FIREBASE_PROJECT_ID=gen-lang-client-0071017187 がある状態で
  2. npm run e2e を実行 → seed店舗が表示されず21件失敗
- 原因調査:
  - scripts/seed.ts は projectId "demo-daychi-coffee-map" に書き込む(SEED_PROJECT_ID)
  - firebase-config.ts はフォールバックで demo-daychi-coffee-map を使うが、
    NEXT_PUBLIC_FIREBASE_PROJECT_ID が設定されていればそちらを優先する
  - playwright.config.ts の dev webServer は NEXT_PUBLIC_USE_EMULATOR=true は明示するが
    projectId は明示していないため、Next.jsが自動読込する .env.local の
    projectId=gen-lang-client-0071017187 が有効になる
  - 結果、アプリは Emulator 上の "gen-lang-client-0071017187" 名前空間に接続し、
    seedが書いた "demo-daychi-coffee-map" のデータが見えず0件になる
  - Phase 3までは .env.local が存在せずフォールバックで demo に一致していたため緑だった。
    メインエージェントがWork 1完了時に .env.local(本番projectId入り)を作成して壊れた
- 特定した原因:
  メインエージェントが .env.local に本番projectIdを入れたこと(直接原因)。加えて、
  E2Eのdev webServerがEmulator用projectIdを明示固定しておらず .env.local の影響を受ける
  設計上の脆さ(再発要因)
- 切り出したタスク: 即時=.env.local削除(本番構成値はapphosting.yamlに保持済みのため冗長)。
  再発防止=progress.txt タスク1-6a(E2EのEmulator projectId明示固定)
- 解決日: 2026-09-21
- 解決内容:
  即時: メインエージェントが .env.local を削除(本番構成値は apphosting.yaml に保持済み)。
  再発防止(タスク1-6a): playwright.config.ts の dev webServer env に
  NEXT_PUBLIC_FIREBASE_PROJECT_ID=demo-daychi-coffee-map を明示固定(コミット f55bcd9)。
  .env.local が無い状態・別projectIdを書いた状態の両方で npm run e2e が68 passedで全緑
  (外部env非依存)であることを実装・レビュー双方で確認。21件失敗は解消

## P-011: 店舗ピンの位置指定が緯度経度手入力で使いづらい
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: /admin/shops(Work 4 実データ登録中にユーザーが指摘)
- 問題内容:
  想定した動き: 管理者が店舗の位置を簡単に指定できる
  実際の動き: 緯度経度を数値で手入力する必要があり、実運用で非常に入力しづらい
- 再現手順:
  1. /admin/shops で店舗を新規登録しようとする
  2. 位置指定が緯度経度の数値入力になっており、住所から座標を調べて手打ちする必要がある
- 原因調査:
  - タスク2-4では地図ドラッグ+緯度経度数値入力でピン確定する実装にした
  - E2Eの決定性のため数値入力欄を主経路にしていたが、実運用の管理者にとっては
    住所から座標を手で調べる手間が大きい
- 特定した原因:
  住所→座標のジオコーディングを実装しておらず、管理者に座標の手入力を強いていたこと
- 切り出したタスク: progress.txt タスク2-4c(住所ジオコーディングによるピン配置)。
  仕様変更として requirements.md「3.2 管理画面 店舗登録」を更新済み(2026-09-22)
- 解決日: 2026-09-22
- 解決内容:
  /admin/shops に「住所からピンを立てる」ボタンを追加。/api/admin/geocode(管理者トークン認証・
  oembedと同パターン)がNominatimで座標取得しピン配置、ずれは地図ドラッグで微調整。緯度経度の
  数値入力欄はUIから撤去(内部state/Firestore保存は緯度経度のまま)。数値欄撤去に伴い店舗作成を
  伴う全E2Eをgeocodeモック経由に更新(P-009の遠隔座標35.3/139.3も維持)。コミット e759fec。
  npm run e2e 80 passed 2回・detail-sheet --repeat-each=20 80 passed・geocode認可401/403 を
  実装/レビュー双方で確認

## P-012: Nominatimが日本の番地住所を解決できず、有効な都内住所でピンが立たない
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: /api/admin/geocode(タスク2-4cで実装)。Work 4 実データ登録中にユーザーが指摘
- 問題内容:
  想定した動き: 有効な住所を入れたらジオコードで座標が返りピンが立つ
  実際の動き: 「東京都世田谷区北沢3-31-3」など有効な都内住所で「住所から座標が
  見つかりませんでした」と表示されピンが立たない
- 再現手順:
  1. /admin/shops で住所「東京都世田谷区北沢3-31-3」を入力
  2. 「住所からピンを立てる」→ 0件エラー
- 原因調査(実APIで確認):
  - Nominatim(現実装): q=東京都世田谷区北沢3-31-3&limit=1 → [](0件)
  - Nominatim(町名のみ): 町丁目レベルの大まかな座標のみ(番地不可・不正確)
  - 国土地理院(GSI) AddressSearch: 正確な座標 [139.666656, 35.66457]
    「東京都世田谷区北沢三丁目31番3号」まで解決(キー不要・日本特化)
- 特定した原因:
  Nominatim(OpenStreetMap)が日本の番地(丁目-番-号)レベルの住所データをほぼ持たず、
  番地付き住所で0件になること。ジオコーディング提供元の選定ミス(日本向けに不適切)
- 切り出したタスク: progress.txt タスク2-4d(ジオコーダを国土地理院GSIに変更)。
  仕様変更として requirements.md のジオコーダ記述を更新する
- 解決日: 2026-09-22
- 解決内容:
  /api/admin/geocode の問い合わせ先を Nominatim から 国土地理院(GSI)AddressSearch に変更
  (コミット eca1ed4)。GeoJSON [lng,lat] を {lat,lng} に正しく変換(取り違えなし)。
  0件404/空400/失敗502、認可401/403は据え置き。実GSI疎通で「東京都世田谷区北沢3-31-3」
  →lat35.66/lng139.67、道玄坂1-2-3等も解決を実装・レビュー双方で確認。E2Eはモック維持で
  80 passed、detail-sheet --repeat-each=20 で80 passed(P-009維持)。requirements.md更新済み

## P-013: 地図のベクタタイルが描画されず真っ白(ピンだけ表示される)
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: /admin/shops の ShopLocationPicker(ユーザーが本番で確認)。公開ページ地図
  (PublicMap)も同じMAP_STYLE_URLを使うため同様の懸念あり
- 問題内容:
  想定した動き: 地図に道路・地名等が描画され、ピン位置を目視確認できる
  実際の動き: 住所からピンを立てると、真っ白(オフホワイト)な地図にピンだけが立ち、
  背景の地図が描画されないため正しい位置か確認できない
- 再現手順:
  1. /admin/shops で住所入力→「住所からピンを立てる」
  2. ピンは立つが地図が真っ白
- 原因調査(ここまでの確認):
  - maplibre-gl.css は import 済み(ShopLocationPicker/PublicMap 両方)
  - 地図コンテナは h-64(256px)で高さあり
  - MAP_STYLE_URL(https://tiles.openfreemap.org/styles/liberty)は HTTP 200・有効なスタイルJSON。
    背景色 background-color:#f8f4f0(オフホワイト)= ユーザーが見る白と一致。
    ベクタソースは https://tiles.openfreemap.org/planet、sprite/glyphsも同ホスト
  - → スタイルは読めているがベクタタイルが描画されていない。resize/タイル読み込みの
    タイミング問題(mapのコンテナ確定前に初期化しresizeしていない等)が有力だが未確定
  - E2Eは常に tiles.openfreemap.org をブロックしていたため、実タイル描画は未検証だった
    (=この不具合を検知できなかったテスト設計上の盲点)
- 特定した原因:
  MapLibreはベクタタイル(.pbf)解析を専用Web Workerで行うが、そのworkerスクリプトURLが
  Next.js/Turbopack環境で解決できず(defaultWorkerUrlが空文字にフォールバック)Workerが
  起動しなかった。背景・スプライト・ラスタはメインスレッド処理のため表示され、ベクタ(道路・
  地名)だけ描画されなかった。E2Eが常にタイルをブロックしていたため未検知だった
- 切り出したタスク: progress.txt タスク3-1a(地図タイル描画の修正・admin/公開両方)
- 解決日: 2026-09-22
- 解決内容:
  /api/maplibre-worker/[file] Route Handler で maplibre-gl-worker.mjs とその依存
  maplibre-gl-shared.mjs を node_modules から allowlist配信し、地図初期化前に setWorkerUrl で
  指定(ShopLocationPicker/PublicMap 両方)。コミット 2073ec3。実タイルで /admin/shops と / に
  道路・地名が描画されること(.pbf取得200)、ピン位置正常、worker routeのallowlist/パストラバーサル
  防止を実装・レビュー双方で確認。既存モックE2Eは無修正で80 passed維持。
  ※本番App Hosting環境での最終確認は2-8デプロイ後のスモークで実施推奨

## P-014: 実APIでGemini抽出が400エラーになる
- 記録日: 2026-09-23
- 状態: 解決済み
- 発生箇所: src/lib/ai-extraction/gemini-adapter.ts(タスク4-5のローカル試運転で発覚)
- 問題内容:
  想定した動き: 実際のGemini API(gemini-3.6-flash)で概要欄から店舗情報をJSON抽出できる
  実際の動き: 「Gemini APIがエラーを返しました(status: 400)」で抽出が失敗する
- 再現手順:
  1. 実キーで fetchVideoTextContent("S41iyTw1_dg") → getExtractionProvider().extract(...)
  2. 400で失敗
- 原因調査(実APIで確認):
  - アダプタは generationConfig.responseSchema に共通の EXTRACTION_JSON_SCHEMA を渡している
  - 共通スキーマは JSON Schema 形式で addressCandidate に type: ["string","null"](型の配列)を使う
  - Geminiの responseSchema は独自のスキーマ形式で型の配列を受け付けず、
    「Unknown name "type" ... Proto field is not repeating, cannot start list.」で400になる
  - 同じスキーマを generationConfig.responseJsonSchema(JSON Schema形式)で渡すと成功することを実APIで確認
  - ユニットテストはGemini応答をモックしていたため、リクエスト形の誤りを検知できなかった
  - あわせて、住所が本文に無い入力でGeminiが存在しない住所(福岡)を作り出すことを確認した
    (実店舗は東京・下北沢)。住所の作り出し防止をプロンプトで徹底する必要がある
- 特定した原因:
  Geminiの responseSchema(独自形式)に JSON Schema 形式のスキーマを渡していたこと。
  テストがモックのみでリクエスト形式を実APIに照らして検証していなかったこと
- 切り出したタスク: progress.txt タスク4-3b(Geminiのスキーマ指定修正と住所の作り出し防止)
- 解決日: 2026-09-23
- 解決内容:
  gemini-adapter.ts を responseJsonSchema で送る形に変更し、prompt.ts に本文に無い住所・情報を作らない指示を追加
  (コミット 9ed9b31)。実APIで S41iyTw1_dg の抽出が400にならず成功、住所の無い本文で3回とも
  addressCandidate=null を確認(レビューPASS)。※実動画は概要欄に住所・メニューが無く字幕も取れないため
  店名しか抽出できない点は別問題として P-015 で扱う

## P-015: AI抽出で店名しか取れない(住所・飲食メニューが空になる)
- 記録日: 2026-09-23
- 状態: 解決済み
- 発生箇所: src/lib/youtube-transcript.ts / src/lib/ai-extraction/gemini-adapter.ts(タスク4-3bの実API検証で発覚)
- 問題内容:
  想定した動き: 実動画から店名・住所候補・出演者ごとの飲食メニューが抽出され、下書きの手直しが少なくて済む
  実際の動き: S41iyTw1_dg(筋金珈琲焙煎所)で抽出結果が店名のみ。consumptions が空、住所も null
- 再現手順:
  1. 実キーで fetchVideoTextContent("S41iyTw1_dg") → extract
  2. 結果 {"shops":[{"name":"筋金珈琲焙煎所","addressCandidate":null,"consumptions":[]}]}
- 原因調査(実APIで確認):
  - 概要欄(451文字)には店名とHP・出演者SNSしか書かれておらず、住所・飲食メニューの記載が無い
  - YouTube Data API captions.list では字幕は自動生成(ja, asr)のみ。非公式 timedtext の一覧取得は空を返し、
    字幕テキストを取得できない(transcript=null)
  - → AIに渡る本文に住所・メニューの根拠が無いため、作り出し防止(P-014)どおり空で返っていた
  - 代替手段の検証: Gemini の generateContent に動画URL(file_data.file_uri=YouTube URL)を渡し、
    mediaResolution=MEDIA_RESOLUTION_LOW で抽出したところ、店名と出演者3名分「スジガネブレンド」を
    正しく抽出(手入力の正解と一致)。住所は動画内で言及が無く null(正しい)。無料枠キーで成功、
    33分動画で約27秒・入力約18万トークン
  - 同検証中、Geminiの一時的な503(overloaded)が複数回発生
- 特定した原因:
  このチャンネルでは概要欄に店舗詳細が書かれず、字幕も自動生成のみで取得できないため、テキストだけでは
  抽出材料が不足すること(入力ソースの設計が実データに合っていなかった)
- 切り出したタスク: progress.txt タスク4-3c(Gemini抽出を動画入力方式に変更・503リトライ・取り込み中表示)。
  仕様変更として requirements.md「5. AI自動抽出パイプライン」を更新(2026-09-23)
- 解決日: 2026-09-23
- 解決内容:
  Geminiアダプタに動画入力(file_data=YouTube URL、mediaResolution LOW)・503等のリトライ・取り込み中表示を追加
  (4-3c、コミット ed805ae)。実APIで本番の出演者名を使い S41iyTw1_dg を抽出し、成功4回とも店名と3名分の
  「スジガネブレンド」を取得(再レビューPASS)

## P-016: 実APIの抽出で出演者名のゆれ・通信エラー未リトライ・大まかな住所の確定扱い
- 記録日: 2026-09-23
- 状態: 解決済み
- 発生箇所: src/lib/ai-extraction/(gemini-adapter, draft-plan, save-draft)。タスク4-3cのレビューFAIL後の追加調査で発覚
- 問題内容:
  (1) AIが出演者名を一覧どおりに返さないことがある(「高橋さん(カメラマン)」→「高橋さん」)。
      マスタと一致せず未割り当て扱いになり、レビューでの手作業が増える
  (2) 通信自体の失敗(fetchの例外)がリトライされず、1回の失敗で抽出が止まる
  (3) AIが「東京都世田谷区北沢」のような町名止まりの住所を返すとGSIで町の中心付近の座標が取れ、
      locationConfirmed が確定扱いになる。実店舗からずれたピンがそのまま承認できてしまう
- 再現手順:
  1. 本番の出演者名 ["高橋さん(カメラマン)","出井さん","かみちぃさん"] で S41iyTw1_dg を実APIで抽出
  2. 回によって performerName が「高橋さん」になり resolvePerformerId が未一致、または接続失敗で即エラー、
     または addressCandidate が「東京都世田谷区北沢」になる
- 原因調査(実APIで確認):
  - 4-3cレビューのFAILは、レビューが出演者名を「高橋」とだけ渡したため(役割の手がかりが無くカメラ裏の
    人物を拾えなかった)。本番の名前で実行すると成功2回とも3名分を抽出できた(4-3cの目的自体は達成)
  - (1) プロンプトは既知の出演者一覧を渡すが、表記を一覧どおりに返すことを強制していない。
        resolvePerformerId は完全一致(正規化後)のため「さん」・括弧書きの有無で一致しない
  - (2) リトライ対象はHTTPステータス(429/5xx)のみで、fetch自体の例外は対象外
  - (3) GSIの正規化住所は番地まで分かると「〜番」「〜番〜号」、町名・丁目止まりだと「番」を含まない
        (北沢3-31-3→北沢三丁目３１番３号、北沢3丁目→北沢三丁目、北沢→北沢)。現状は座標が取れれば確定扱い
- 特定した原因:
  (1) 出演者名の出力表記を拘束していない+照合が表記ゆれに弱い (2) リトライ条件の漏れ
  (3) 住所の粒度を見ずに確定扱いにしている
- 切り出したタスク: progress.txt タスク4-3d。仕様は requirements.md「5.」下書き保存時の扱いを更新(2026-09-23)
- 解決日: 2026-09-23
- 解決内容:
  出演者名を一覧どおりに返させる指示+スキーマの候補提示、敬称・括弧書きを吸収する照合、通信失敗のリトライ、
  正規化住所に「番」を含む時だけ位置確定(4-3d、コミット 1a93bc8)。実APIで3名ともマスタ一致、北沢→false/
  北沢3-31-3→true を確認(レビューPASS)

## P-017: Gemini無料枠の1日上限(20回)に達すると分かりにくいエラーで止まり、リトライで回数を浪費する
- 記録日: 2026-09-23
- 状態: 解決済み
- 発生箇所: src/lib/ai-extraction/gemini-adapter.ts / src/app/admin/import/page.tsx(4-3dの実API検証・追加調査で発覚)
- 問題内容:
  想定した動き: 利用上限に当たった時、待てば回復する制限なら待って再試行し、その日はもう使えない制限なら
                すぐ止めて管理者に分かる言葉で伝える
  実際の動き: 429はすべて1秒→2秒→4秒の待機で再試行し、失敗すると「Gemini APIがエラーを返しました(status: 429)」
              とだけ表示される。1日の上限でも再試行して回数を浪費する
- 再現手順:
  1. 同じ日に gemini-3.6-flash を20回呼んだ後、取り込みを実行
  2. 429(RESOURCE_EXHAUSTED)で失敗し、分かりにくいメッセージが出る
- 原因調査(実APIで確認):
  - 429の応答本文 error.details に以下が含まれる:
    - google.rpc.QuotaFailure: quotaId "GenerateRequestsPerDayPerProjectPerModel-FreeTier", quotaValue "20"
      (無料枠は1プロジェクト・1モデルあたり1日20リクエスト)
    - google.rpc.RetryInfo: retryDelay "37s"(1日上限でも短い待機時間が返るため、そのまま信用すると無駄に再試行する)
  - 4-3dレビューで6回中2回失敗した429、および今回の3連続429はこの1日上限/短時間の制限によるもの
  - 1日上限は太平洋時間0時(日本時間16時ごろ)にリセットされる(Googleのドキュメントの説明による)
- 特定した原因:
  429の中身(1日上限か短時間の制限か、推奨待機時間)を見ずに一律の短い指数バックオフで再試行していること、
  および失敗理由を管理者に分かる形で伝えていないこと
- 切り出したタスク: progress.txt タスク4-3e
- 解決日: 2026-09-24
- 解決内容:
  429の error.details を解析し、1日上限(quotaId に PerDay)は再試行せず GeminiDailyQuotaExceededError で即失敗し
  quotaValue を使った分かるメッセージを表示。短時間制限は RetryInfo.retryDelay(上限90秒)待機後に再試行。
  取り込み画面は1日上限で残りの動画を処理せず理由を表示(4-3e、コミット 42e103d)。ユニット・E2E(108 passed×2)で
  確認(レビューPASS)。実APIでの挙動確認は4-5で行う

## P-018: E2Eの詳細シートテストが稀に訪問2件を検出して失敗する(テスト間の共有データ汚染)
- 記録日: 2026-09-23
- 状態: 解決済み
- 発生箇所: e2e/detail-sheet.spec.ts「published店舗のピンをクリック…」(113-114行 detail-sheet-visit を1件と期待)
  (5-1実装時のフルE2E実行で報告。本タスク変更を除いた元コードでも再現することを実装担当が確認)
- 問題内容:
  想定した動き: 各E2Eテストは他のテストの実行状況に関係なく同じ結果になる
  実際の動き: フルE2E実行中、まれに detail-sheet-visit が2件検出され失敗する
- 再現手順: npm run e2e を複数回実行(並列実行のタイミング次第で発生)
- 原因調査:
  - e2e/admin-import-existing-shop.spec.ts(4-4)は seed の共有店舗 shop-test-published-01 に紐づく
    published 訪問を「承認」で作成し、テスト末尾(151-167行)で訪問・動画を削除している。
    実装担当の報告「後片付けしない」は誤りで、後片付け自体はある
  - playwright.config.ts は fullyParallel:true かつ desktop/mobile の2プロジェクトで、別ワーカーが同時に走る。
    このため「承認〜後片付け」の数秒間に detail-sheet.spec.ts が同じ店舗の詳細シートを開くと訪問2件になる
  - さらに import テストが途中で失敗すると後片付けまで到達せず、訪問が残って以降の実行でも失敗し続ける
- 特定した原因:
  書き込みを伴うテスト(admin-import-existing-shop)が、読み取り前提の他テストと共有するseed店舗を
  並列実行中に変更していること(後片付けはテスト本文末尾にあり、失敗時にも実行されない)
- 切り出したタスク: progress.txt タスク5-1a
- 解決日: 2026-09-24
- 解決内容:
  admin-import-existing-shop.spec.ts を専用published店舗 shop-test-published-import-01(seed追加)に切り替え、
  作成した訪問・動画を try/finally でFirestore直接削除(失敗時も実行)。フルE2E連続4回全緑、
  意図的失敗時もデータが残らないことをレビューで確認(5-1a、レビューPASS)
