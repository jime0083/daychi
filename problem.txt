# Daychi COFFEE MAP - 問題管理 (problem.txt)
# 運用ルール:
# - 開発中に見つかった問題(想定した動きをしない、デザイン崩れ等)は全てここに記録する
# - 修正プロセス(CLAUDE.md「問題修正プロセス」参照・省略禁止):
#   問題共有 → problem.txtに記録 → 原因を徹底調査 → 原因特定 →
#   タスクを切り出しprogress.txt/manual-work.txtに追加 → 修正作業
# - 問題IDは P-001 から連番で付与する
# - 原因調査・特定・切り出しタスクの欄を埋めずに修正作業を開始してはならない

=====================================
記録フォーマット(コピーして使用)
=====================================
## P-XXX: (問題タイトル)
- 記録日: YYYY-MM-DD
- 状態: 未着手 | 調査中 | 修正中 | 解決済み
- 発生箇所: (画面/機能/タスクID)
- 問題内容:
  (想定した動き / 実際の動き を具体的に)
- 再現手順:
  1. ...
- 原因調査:
  (調査した内容・確認したコード・データフローの追跡結果)
- 特定した原因:
  (根本原因。調査完了までは空欄)
- 切り出したタスク:
  (progress.txt / manual-work.txt に追加したタスクID)
- 解決日: YYYY-MM-DD
- 解決内容:
  (実施した修正と検証結果)

=====================================
問題一覧
=====================================

## P-001: Firestore Emulatorに必要なJava実行環境がPATHに存在しない
- 記録日: 2026-09-19
- 状態: 解決済み
- 発生箇所: タスク1-3(Firebase Emulator Suite設定)
- 問題内容:
  想定した動き: java -version が通り、Firestore Emulator(JRE依存)が起動できる
  実際の動き: 「Unable to locate a Java Runtime.」でjavaコマンドが失敗し、Emulatorが起動できない
- 再現手順:
  1. ターミナルで java -version を実行 → エラー
- 原因調査:
  - /usr/libexec/java_home も失敗し、システム標準のJavaランタイムは未インストール
  - Homebrewでは /opt/homebrew/opt/openjdk (openjdk 26.0.1) がインストール済みで、
    直接パス指定での動作確認は成功(/opt/homebrew/opt/openjdk/libexec/openjdk.jdk/Contents/Home/bin/java)
  - ただし brew link されておらず /opt/homebrew/bin に露出していないためPATHから解決できない
- 特定した原因:
  Homebrew版openjdkがインストール済みだが未リンク(keg-only)のため、javaコマンドがPATH上に存在しない
- 切り出したタスク: manual-work.txt Work 9(Java実行環境のPATH設定)
- 解決日: 2026-09-19
- 解決内容:
  ユーザー承認のうえ brew link --force --overwrite openjdk を実行(37 symlinks created)。
  java -version で openjdk 26.0.1 が返ることを確認。状態を「解決済み」とする

## P-002: クリーンな状態で npx tsc --noEmit が LayoutProps 未定義エラーで失敗する
- 記録日: 2026-09-19
- 状態: 解決済み
- 発生箇所: src/app/layout.tsx:20(タスク1-3実装中に発覚。1-3の変更とは無関係)
- 問題内容:
  想定した動き: 完了前必須チェックの npx tsc --noEmit がいつでもパスする
  実際の動き: .next ディレクトリが無い状態では
  「src/app/layout.tsx(20,50): error TS2304: Cannot find name 'LayoutProps'.」で失敗する
- 再現手順:
  1. .next ディレクトリを削除(またはクリーンな状態でclone)
  2. npx tsc --noEmit を実行 → TS2304エラー
- 原因調査:
  - layout.tsx:20 は create-next-app(Next.js 16)標準テンプレートのまま LayoutProps<"/"> を使用
  - LayoutProps は Next.js 16 が next dev / next build / next typegen 実行時に
    .next/types へ自動生成するグローバル型(tsconfigのincludeで取り込まれる)
  - 現在 .next が存在しないことを確認し、エラーを再現
  - タスク1-1/1-2の検証時は devサーバー起動後(.next生成済み)だったため偶然パスしていた
- 特定した原因:
  自動生成型 LayoutProps に依存するコードに対し、型生成(next typegen)を伴わない
  素の tsc --noEmit を実行していたため。チェックコマンド側の手順不足が根本原因
- 切り出したタスク: progress.txt タスク1-7(型チェックコマンドの安定化)
- 解決日: 2026-09-19
- 解決内容:
  npm script "typecheck"(next typegen && tsc --noEmit)を追加(コミット c863078)。
  .next 削除済みのクリーン状態から npm run typecheck が2回連続でパスすることを
  実装・レビュー双方で確認。以後の型チェックは npm run typecheck に統一
  (CLAUDE.mdのコマンド表も更新済み)

## P-003: セキュリティルール適用後にseedスクリプトがPERMISSION_DENIEDで失敗
- 記録日: 2026-09-19
- 状態: 解決済み
- 発生箇所: scripts/seed.ts(タスク1-6のnpm run e2e初回実行時に顕在化)
- 問題内容:
  想定した動き: npm run e2e のglobalSetupでseedデータがEmulatorに投入される
  実際の動き: seed投入が PERMISSION_DENIED で失敗する
- 再現手順:
  1. タスク1-5適用後(firestore.rules本実装後)の状態で emulator起動
  2. npm run seed を実行 → PERMISSION_DENIED
- 原因調査:
  - タスク1-5の firestore.rules は write を管理者(admin==trueクレーム)のみに制限
  - タスク1-3作成の scripts/seed.ts は未認証のclient SDKで書き込む実装だった
  - 1-5のレビューではリポジトリテスト(管理者サインイン済み)のみ通しで確認され、
    seedスクリプト単体はルール適用後に未検証だったため、1-6の全自動フローで初めて顕在化
- 特定した原因:
  firestore.rules(1-5)と seed.ts(1-3)の設計不整合。seed.tsが無認証書き込みのままだった
- 切り出したタスク: タスク1-6の範囲内で修正(E2E基盤の完了条件に必須のため)
- 解決日: 2026-09-19
- 解決内容:
  scripts/seed.ts を @firebase/rules-unit-testing の withSecurityRulesDisabled()方式
  (ルールテストで確立済みのパターン)に変更。あわせてFirestore接続残留でnodeが
  終了しない問題に process.exit() を明示。npm run e2e 3回連続グリーンで解決確認。
  検証はタスク1-6のレビュー(daychi-review)に含めて実施

## P-004: npm run lint が e2e/artifacts の生成物を走査して大量エラーになる
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: eslint.config.mjs(タスク2-1の検証中に発覚)
- 問題内容:
  想定した動き: npm run lint がプロジェクトのソースのみを対象にパスする
  実際の動き: npm run e2e 実行後、e2e/artifacts/html-report 配下のPlaywright生成JS
  (圧縮済み)をESLintが走査し、259エラー・約2795警告で失敗する
- 再現手順:
  1. npm run e2e を1回実行(e2e/artifacts/html-report が生成される)
  2. npm run lint を実行 → 生成物由来の大量エラーで失敗
- 原因調査:
  - eslint.config.mjs の globalIgnores は .next/**, out/**, build/**, next-env.d.ts のみ
    を除外しており、e2e/artifacts/** が除外対象に入っていない
  - ESLint(flat config)は .gitignore を自動参照しないため、gitignore済みでも走査される
  - npx eslint src e2e --ignore-pattern 'e2e/artifacts/**' は exit 0(2-1コード自体は健全)
    → 失敗はすべて生成物由来と確認
  - 1-6のレビューでlintがパスしたのは、lint実行時点でartifactsが未生成だった等の
    実行順序に依存していたため。恒常的な設定不足が根本原因
- 特定した原因:
  eslint.config.mjs の除外リストに e2e/artifacts(および playwright-report / test-results)
  が含まれていないこと。lintの対象範囲設定の不足
- 切り出したタスク: progress.txt タスク1-8(lint除外設定の修正)
- 解決日: 2026-09-22
- 解決内容:
  eslint.config.mjs の globalIgnores に e2e/artifacts/**、playwright-report/**、
  test-results/** を追加(コミット b75cc29)。artifacts生成済みの状態で npm run lint が
  exit 0 でパスすること、実コード(src/e2e配下)は引き続きlint対象であることを
  実装・レビュー双方で確認

## P-005: /admin がクライアント側で auth/invalid-api-key でクラッシュする
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: src/lib/firebase-config.ts(Phase 1のタスク1-2/1-4由来の潜在バグ。タスク2-1のE2E 6件失敗として顕在化)
- 問題内容:
  想定した動き: /admin がブラウザで表示され、ログイン/アクセス拒否/管理画面の3分岐が動く
  実際の動き: クライアント側レンダリング時に getAuth() が auth/invalid-api-key を投げ、
  UIが一切描画される前にページ全体がクラッシュ。admin-auth.spec.ts が6件(3シナリオ×2プロジェクト)失敗
- 再現手順:
  1. NEXT_PUBLIC_USE_EMULATOR=true で npm run e2e を実行
  2. admin-auth.spec.ts の全シナリオが同一原因で失敗
- 原因調査:
  - firebase-config.ts の関数群が env: NodeJS.ProcessEnv = process.env を引数に取り、
    本体で env.NEXT_PUBLIC_X と間接参照していた
  - Next.js/Turbopack はクライアントバンドルへ「リテラルな process.env.NEXT_PUBLIC_X」
    のみをインライン展開する。エイリアス経由(env.NEXT_PUBLIC_X)は静的解析で見えず
    undefined になる
  - 結果、ブラウザ側で全 NEXT_PUBLIC_* が undefined となり getAuth() が例外
  - Phase 1ではクライアントからこの初期化を通る画面が無かったため潜在化していた
- 特定した原因:
  クライアント向けFirebase設定の環境変数を間接参照していたため、Next.jsの静的置換が
  効かずクライアントバンドルで値が欠落したこと
- 切り出したタスク: タスク2-1の範囲内で修正(2-1の完了条件E2Eに必須のため)
- 解決日: 2026-09-22
- 解決内容:
  firebase-config.ts を、リテラルな process.env.NEXT_PUBLIC_X を直接書く readEnv()
  ヘルパー方式に変更(呼び出しごとに評価。テストのprocess.env変更も反映)。
  Auth Emulatorモード用に demo API key フォールバックも追加。npm run e2e 8件全通過で
  解決確認。検証はタスク2-1のレビュー(daychi-review)に含めて実施。
  【今後の注意】クライアント側で NEXT_PUBLIC_* を読む場合は必ずリテラル参照にすること
  (2-2以降で新たな env 依存のクライアント設定を追加する場合に留意)

## P-006: maplibre-gl@5.24.0 に本番依存の critical 脆弱性(XSS)
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: package.json の dependencies(タスク2-4で追加)。タスク2-4のレビューでFAIL事由として検出
- 問題内容:
  想定した動き: 本番依存に critical/high 脆弱性を持ち込まない
  実際の動き: タスク2-4で追加した maplibre-gl@5.24.0 に critical 脆弱性
  GHSA-jrc7-96c5-q579(XSS Sanitizer Bypass in DOM.sanitize())が含まれる
- 再現手順:
  1. npm audit を実行 → critical 1件(maplibre-gl@5.24.0、isDirect:true、dependencies)
- 原因調査:
  - タスク2-4(コミット 8351293)で地図UI用に maplibre-gl@^5.24.0 を追加した
  - 5.24.0 は GHSA-jrc7-96c5-q579 の影響を受けるバージョン
  - 現状の実使用での実害経路は低い(Popup.setHTMLで未信頼HTMLを描画しておらず、
    draggable Marker と信頼できるOpenFreeMapスタイルURLのみ使用)が、脆弱コードは
    本番に同梱され、Phase 3(3-1 公開ページ地図)でも再利用されるため放置不可
  - moderate 8件は firebase-tools(dev依存、Phase 1導入)配下のtransitiveで本番非同梱・
    本問題とは別事象(別途 dev依存更新方針を検討。P-006の対象外)
- 特定した原因:
  脆弱性を含む maplibre-gl@5.24.0 を本番依存に追加したこと
- 切り出したタスク: progress.txt タスク2-4a(maplibre-gl のセキュリティ更新)
- 解決日: 2026-09-22
- 解決内容:
  maplibre-gl を 5.24.0 → 6.10.0 へ更新(コミット b87a467)。v6はESM/named export専用の
  ため ShopLocationPicker.tsx の import を「import * as maplibregl」に追随修正(それ以外の
  地図API使用箇所は無変更)。npm audit で critical 0件を確認。npm run e2e(desktop+mobile
  14件)2回連続グリーン、実ブラウザで地図描画・座標反映・SSRエラー無しをレビューで確認。
  moderate 8件(firebase-tools系dev依存)はP-006対象外の既存事象として別途方針検討

## P-007: performers-crud.spec.ts の表示順アサーションがフレーキー
- 記録日: 2026-09-22
- 状態: 解決済み
- 発生箇所: e2e/performers-crud.spec.ts(タスク2-2で作成)。タスク2-6のレビュー中に発覚
- 問題内容:
  想定した動き: E2Eが決定的に(毎回)グリーンになる
  実際の動き: performers-crud(mobile)が約0.4%の確率でstrict modeのタイムアウトで失敗
- 再現手順:
  1. npm run e2e を多数回実行すると、まれに performers-crud.spec.ts が失敗する
- 原因調査:
  - performers-crud.spec.ts:49 で表示順に固定値 "42" を入力し、row.getByText("42") で
    部分一致検索している
  - uniqueTestId()(src/repositories/test-support.ts:65)が生成するランダムID
    (例 1789827262544-42c0xi)がたまたま "42" を部分文字列として含むと、
    Playwrightのstrict modeが2要素にマッチしてタイムアウトする
  - タスク2-6のdiffとは無関係の既存事象(コミット40766e8/タスク2-2由来)
- 特定した原因:
  テストの表示順アサーションが厳密ロケータでなく部分一致テキスト検索であり、
  ランダムIDと衝突しうること(テストコードの堅牢性不足)
- 切り出したタスク: progress.txt タスク2-2a(performers-crud E2Eのフレーキー修正)
- 解決日: 2026-09-22
- 解決内容:
  performers/page.tsx の表示順セルに data-testid="performer-order" を付与し、
  performers-crud.spec.ts のアサーションを row.getByTestId("performer-order") +
  toHaveText の厳密一致に変更(部分一致 getByText を廃止)。入力値も "42"→"917" に変更。
  コミット 72cea1b。npm run e2e を3回連続実行し22件グリーン(フレーキー再発なし)を
  実装・レビュー双方で確認

## P-008: next dev の Issues バッジが下部タブバーと重なりE2Eクリックを妨げる
- 記録日: 2026-09-20
- 状態: 解決済み(回避策適用、本番影響なし)
- 発生箇所: e2e/mobile-ui.spec.ts のタブクリック(タスク3-5実装中に発覚)
- 問題内容:
  想定した動き: モバイル下部タブ「地図」「動画一覧」のクリックがE2Eで安定して通る
  実際の動き: Next.js の next dev 専用「Issues」インジケータバッジ(左下)が
  下部タブバーと重なり、Playwrightの座標ベースクリックを間欠的にintercept してタイムアウト
- 再現手順:
  1. npm run e2e でモバイルのタブクリックを座標ベースで行うと間欠的に失敗
- 原因調査:
  - 既存の全mapスペックが地図タイル(tiles.openfreemap.org)を意図的にブロックしており、
    その結果 next dev の「Issues」バッジが表示される
  - このバッジは画面左下に固定表示され、下部タブバーと表示領域が重なる
  - next.config.ts の devIndicators: false では Next.js 16.3.5 でこのバッジを抑制できないことを
    実験で確認。.click({ force: true }) も座標ヒットテストを回避するだけで実クリックは最上位要素に当たり不可
  - バッジは next dev 専用で本番ビルドには存在しない(=本番のタブバー操作性には影響しない)
- 特定した原因:
  next dev 専用の開発用オーバーレイ(Issuesバッジ)がテスト環境でのみタブバーと重なること。
  本番コードの欠陥ではなくテスト環境固有の事象
- 切り出したタスク: タスク3-5の範囲内で対応(テスト専用ヘルパーで回避)
- 解決日: 2026-09-20
- 解決内容:
  e2e/mobile-ui.spec.ts にテスト専用ヘルパー clickBypassingDevOverlay() を追加し、
  対象DOMノードのネイティブ .click() を直接呼ぶ方式でタブバー2ボタンのみ回避。
  本番ビルドにバッジは存在しないため本番影響なし。回避が本番の実レイアウト欠陥を
  隠していないことはタスク3-5のレビュー(daychi-review)で本番ビルド観点も含めて検証

## P-009: detail-sheet.spec.ts が動的作成店舗とseed店舗のピン座標重なりで間欠失敗
- 記録日: 2026-09-20
- 状態: 修正中
- 発生箇所: e2e/detail-sheet.spec.ts:150 付近(タスク3-2由来。タスク3-5のレビュー中に発覚)
- 問題内容:
  想定した動き: detail-sheet の複数visitテストが決定的に(毎回)通る
  実際の動き: 5回中1回程度、chromium-desktop でピンクリックがタイムアウトして失敗
- 再現手順:
  1. npm run e2e を5回程度繰り返すと稀に detail-sheet.spec.ts が失敗する
  2. または npx playwright test e2e/detail-sheet.spec.ts --repeat-each=20 --project=chromium-desktop
- 原因調査:
  - 複数visitテストは管理画面CRUDで新規店舗を動的作成する。座標にジッターを入れているが
    ランダムに seed店舗(shop-test-published-01)のピンと画面座標が近接/重なることがある
  - ピンが重なるとPlaywrightのクリックが目的のピンに届かずintercept/タイムアウトする
  - 3-4のフィルタ帯レイアウト修正後も、ピン同士の重なり自体は残存(フィルタとの重なりではなく
    ピン同士の重なりが原因)
- 特定した原因:
  テストデータの店舗座標が非決定的(ジッター)で、seed店舗ピンと画面上で重なりうること。
  テストの座標設計の堅牢性不足(本番コードの欠陥ではない)
- 切り出したタスク: progress.txt タスク3-2b(detail-sheet 複数visitテストの座標決定化)
- 解決日:
- 解決内容:
